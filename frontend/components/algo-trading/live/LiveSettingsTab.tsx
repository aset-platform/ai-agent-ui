"use client";

/**
 * LiveSettingsTab — placeholder. The full body (KillSwitchToggle,
 * ReconciliationDriftPanel, LiveModeToggle, dry-run arm widget)
 * is wired up in Slice 5 of the three-page split.
 */
export function LiveSettingsTab() {
  return (
    <p
      className="text-sm text-slate-500"
      data-testid="live-settings-placeholder"
    >
      Live Settings — wired up in Slice 5.
    </p>
  );
}
