"""Bundled strategy templates (REGIME-7).

JSON files in this directory are reference / gallery strategies
exercising the v3 grammar (regime overlay + factor library +
vol-target sizing + PIT universe). Loaded via ``loader``.
"""
